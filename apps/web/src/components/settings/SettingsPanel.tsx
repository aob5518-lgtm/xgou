'use client';

import { useState } from 'react';
const rows = ['Reduced Motion', 'Notifications'] as const;
export function SettingsPanel() { const [values, setValues] = useState<Record<string, boolean>>({ 'Reduced Motion': false, Notifications: true }); return <div className="glass overflow-hidden rounded-2xl"><div className="grid gap-2 border-b border-[var(--border)] p-5 sm:grid-cols-[220px_1fr]"><span className="text-sm">Connected Wallet</span><span className="text-sm text-white/40">Not connected · display only</span></div><div className="grid gap-2 border-b border-[var(--border)] p-5 sm:grid-cols-[220px_1fr]"><span className="text-sm">Network</span><span className="text-sm text-white/40">Arc Demo Preview</span></div><div className="grid gap-2 border-b border-[var(--border)] p-5 sm:grid-cols-[220px_1fr]"><span className="text-sm">Language</span><span className="text-sm text-white/40">中文 / English</span></div>{rows.map((row) => <div key={row} className="flex items-center justify-between border-b border-[var(--border)] p-5"><span className="text-sm">{row}</span><button type="button" aria-label={row} aria-pressed={values[row]} onClick={() => { setValues((current) => ({ ...current, [row]: !current[row] })); }} className={`relative h-7 w-12 rounded-full transition ${values[row] ? 'bg-[var(--cyan)]' : 'bg-white/10'}`}><i className={`absolute top-1 size-5 rounded-full bg-white transition ${values[row] ? 'left-6' : 'left-1'}`} /></button></div>)}<div className="flex items-center justify-between p-5"><span className="text-sm">Demo Mode</span><span className="rounded-full bg-[var(--warning)]/10 px-3 py-1 text-[10px] text-[var(--warning)]">ENABLED · LOCKED</span></div></div>; }
