import { MetricCard } from '@/components/dashboard/MetricCard';

export function StrategyCard({ name, allocation, tone, metrics }: { readonly name: string; readonly allocation: string; readonly tone: 'cyan' | 'blue'; readonly metrics: readonly { label: string; value: string }[] }) {
  const status = name.includes('SPOT') ? ['SIGNAL · NEUTRAL / LONG BIAS', 'EXPOSURE · 62%', 'RISK · MEDIUM'] : ['TREND · BULLISH', 'LEVERAGE · 1.8×', 'RISK · CONTROLLED'];
  return <section className={`glass rounded-2xl p-6 ${tone === 'cyan' ? 'cyan-glow' : 'blue-glow'}`}><div className="flex items-end justify-between"><div><p className="eyebrow">{name.replace('STRATEGY', 'ENGINE')}</p><h2 className="mt-2 text-3xl font-light">{allocation}</h2></div><span className="flex items-center gap-2 text-[9px] tracking-wider text-[var(--success)]"><i className="system-pulse size-1.5 rounded-full bg-[var(--success)]" />ONLINE</span></div><div className="mt-5 flex flex-wrap gap-x-5 gap-y-2 border-y border-white/[.05] py-3">{status.map((item) => <span key={item} className="text-[8px] tracking-[.12em] text-white/40">{item}</span>)}</div><div className="mt-5 grid grid-cols-2 gap-3 xl:grid-cols-3">{metrics.map((metric) => metric.label === 'RISK' ? <div key={metric.label} className="glass rounded-2xl bg-white/[.012] p-5"><p className="eyebrow">RISK</p><p className="mt-4 flex items-center gap-2 text-[10px] tracking-[.13em] text-[var(--success)]"><i className="size-1.5 rounded-full bg-[var(--success)]" />{metric.value}</p></div> : <MetricCard key={metric.label} label={metric.label} value={metric.value} className="bg-white/[.012]" />)}</div></section>;
}
