import { PageHeader } from '@/components/layout/PageHeader';
import { SettingsPanel } from '@/components/settings/SettingsPanel';
export default function SettingsPage() { return <><PageHeader eyebrow="PREVIEW PREFERENCES" title="Settings" description="Interface-only controls. No network switching or financial permissions are requested." /><SettingsPanel /></>; }
